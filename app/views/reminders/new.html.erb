<div class="w-full max-w-2xl mx-auto">
  <div class="mb-6">
    <%= link_to reminders_path, class: "text-sm text-gray-500 hover:text-gray-900" do %>
      ← All reminders
    <% end %>
  </div>
  <h1 class="text-2xl font-semibold text-gray-900 tracking-tight mb-8">New reminder</h1>
  <%= render "form" %>
</div>
